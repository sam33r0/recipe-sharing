{
    "version": 2,
    "builds":[
        {
            "src":"./src/app.js",
            "use":"@vercel/node -r dotenv/config --experimental-json-modules"
        }
    ],
    "routes":[
        {
            "src": "/(.*)",
            "dest":"/"
        }
    ]
}